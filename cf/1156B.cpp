#include<bits/stdc++.h>
using namespace std;


typedef long long ll;

typedef pair<int,int> ii;
vector<string> a;
int n;
string op(string s) {
    sort(s.begin(), s.end());
    vector<

}

void solve() {

    
}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    cin >> n;
    a.resize(n);
    for(auto &e : a) cin >> e;
    
    solve();
}